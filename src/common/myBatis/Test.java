package common.myBatis;


public class Test {

	/**
	 * @param args
	 */
	// public static void main(String[] args) {
	// String dbName = "tyreal_dev";
	// SqlSession sqlSession = Session.getSqlSessionFactory(dbName)
	// .openSession();
	// DataDaoMapper dao = sqlSession.getMapper(DataDaoMapper.class);
	// // query(dao);
	// execProcedure(dao);
	// sqlSession.clearCache();
	// sqlSession.close();
	// }
	//
	// @SuppressWarnings("unchecked")
	// private static void execProcedure(DataDaoMapper dao) {
	// Map<String, String> params = new HashMap<String, String>();
	// params.put("inParam", "aaa");
	// // dao.execSMS(params);
	// Iterator<?> it = params.entrySet().iterator();
	// while (it.hasNext()) {
	// Entry<String, String> entry = (Entry<String, String>) it.next();
	// System.out.println(entry.getKey() + ":" + entry.getValue());
	// }
	// }
	//
	// private static void query(DataDaoMapper dao) {
	// Proceduretask pt = new Proceduretask();
	// pt.setProcedurename("t2");
	// // int count = dao.getSjshbCount(pt);
	// // System.out.println(count);
	//	}
}
